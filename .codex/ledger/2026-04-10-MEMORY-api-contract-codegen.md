Goal (incl. success criteria):

- Eliminate manual TypeScript contract drift by generating REST contracts from Go into OpenAPI/TypeScript for the web, and generating extension SDK contracts from Go into TypeScript for the SDK.
- Success means: Go owns the canonical REST + extension wire contracts; generated artifacts are committed and reproducible; web REST consumers no longer hand-maintain mirrored DTO/Zod shapes for backend contracts; SDK contract types come from generated output; stale generated files fail verification; `make verify` passes.

Constraints/Assumptions:

- Follow root `AGENTS.md` / `CLAUDE.md`, plus the named skills `exa-web-search-free`, `no-workarounds`, and `systematic-debugging`.
- Do not touch unrelated worktree changes already present in `.compozy/tasks/*`, `docs/ideas/anp/*`, and other modified files outside this task’s scope.
- Keep transport-local HTTP prompt/SSE payloads out of the shared REST OpenAPI contract.
- REST uses OpenAPI as the generated interchange format; extensions use a sibling Go->TS generator because Host API/hooks are JSON-RPC, not REST.

Key decisions:

- Use `kin-openapi`/`openapi3gen` on the Go side for OpenAPI generation and `openapi-typescript` + `openapi-fetch` on the TS side for REST consumers.
- Keep the frontend’s TanStack Query structure (`queryKeys`/`queryOptions`) and swap its underlying fetch/type source to generated OpenAPI clients instead of hand-written request/response typing.
- Extract extension Host API params/results and protocol handshake types into exportable Go contract packages before generating TS, rather than trying to generate from ad-hoc unexported structs.
- Do not adopt annotation-driven Swagger tooling; the contract must stay code-first from actual Go types/route registry.

State:

- completed

Done:

- Investigated current repo state and confirmed root cause: `internal/api/contract` is canonical for daemon REST DTOs, but `web/src/systems/*/types.ts` and `sdk/typescript/src/types.ts` manually mirror backend contracts and have already drifted.
- Verified external tooling direction from primary docs: `openapi-typescript`, `openapi-fetch`, and Orval’s Zod guidance as an optional future extension, not the v1 baseline.
- Read relevant ledgers for API contracts, extension protocol, and extension SDK context.
- Identified unrelated dirty worktree files that must remain untouched.
- Added Go-side OpenAPI generation and extension SDK TypeScript generation, plus committed generated artifacts under `openapi/`, `web/src/generated/`, and `sdk/typescript/src/generated/`.
- Fixed OpenAPI schema required/enums generation so generated web types preserve required session fields and enum unions from Go.
- Migrated web REST adapters and type aliases to generated OpenAPI contracts, keeping only UI-local/session-local types outside the shared REST contract.
- Fixed a real runtime regression in the new `openapi-fetch` adapters by treating non-OK responses as failures even when the response body is empty.
- Rewrote the adapter tests around `Request`/`Response` semantics and added shared fetch test helpers.
- Added `mage Codegen` and `mage CodegenCheck`, with formatted OpenAPI TS output so verify and lint agree on canonical generated bytes.
- Repaired downstream Go call sites and tests that still assumed plain `string` session states or stringly typed granted actions.
- Removed untracked SDK build artifacts produced during local verification.
- Passed `make verify` successfully after the full migration.
- Added explicit `make codegen` / `make codegen-check` targets and wired root `package.json` scripts to them.
- Tightened build/dev integration so `web` and `sdk/typescript` run root codegen checks from their own package scripts, and `mage` now runs raw web scripts after an explicit root `CodegenCheck`.
- Added Turborepo `globalDependencies` for Go-side contract/codegen sources so cached `turbo run build/typecheck/test` paths invalidate when canonical Go contracts or generators change.
- Discovered and fixed two hidden SDK drift issues while wiring direct package `typecheck`: missing generated `HookEventFamily` / `HookRunOutcome` / `HookSkillSource` exports, and an over-narrow protocol-version validation constant in `sdk/typescript/src/extension.ts`.

Now:

- Task complete; integration and verification are finished.

Next:

- None.

Open questions (UNCONFIRMED if needed):

- None blocking.

Working set (files/ids/commands):

- `.codex/ledger/2026-04-10-MEMORY-api-contract-codegen.md`
- `.codex/plans/2026-04-10-api-contract-codegen.md`
- `internal/api/contract/*`
- `internal/api/core/*`
- `internal/api/httpapi/routes.go`
- `internal/api/udsapi/routes.go`
- `internal/subprocess/handshake.go`
- `internal/extension/host_api.go`
- `internal/hooks/*`
- `magefile.go`
- `package.json`
- `web/src/systems/*`
- `sdk/typescript/src/*`
- `sdk/typescript/package.json`
- `web/package.json`
- `package.json`
- `turbo.json`
- `Makefile`
- Commands: `rg`, `sed`, `go test`, `bun run`, `go run github.com/magefile/mage Codegen`, `go run github.com/magefile/mage CodegenCheck`, `make codegen`, `make codegen-check`, `bunx turbo run build --dry=json`, `make verify`
