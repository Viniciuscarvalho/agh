## Goal (incl. success criteria):

- Inspecionar a KB `ai-harness` e materiais locais do AGH sobre harness/extensibility para integrações externas.
- Entregar uma lista curta de padrões/riscos úteis para revisar `.compozy/tasks/channel-adapters/_techspec.md`, com citações locais sempre que possível.

## Constraints/Assumptions:

- Sem editar arquivos de produto; pesquisa documental apenas.
- Usar fontes locais primeiro.
- Localização exata da KB `ai-harness` está UNCONFIRMED no início da sessão.

## Key decisions:

- Usar busca local (`rg`) e skill `qmd` para localizar documentação/KB.
- Considerar docs de protocolo/subprocesso/streaming do próprio AGH como fonte primária.

## State:

- Em andamento.

## Done:

- Lidas instruções de workspace e skill `qmd`.
- Verificado que `.codex/ledger/` não tinha outros ledgers.

## Now:

- Localizar a KB `ai-harness` e documentos AGH relevantes.

## Next:

- Ler os documentos mais próximos do tema e extrair padrões/riscos.

## Open questions (UNCONFIRMED if needed):

- UNCONFIRMED: a KB `ai-harness` está indexada no QMD, em pasta local fora do repo, ou em ambos?

## Working set (files/ids/commands):

- `AGENTS.md`
- `.agents/skills/qmd/SKILL.md`
- `rg --files`
