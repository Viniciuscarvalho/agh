Goal (incl. success criteria):

- Remediate the full scoped CodeRabbit batch for `.compozy/tasks/e2e/reviews-001` covering issues `001` through `020`.
- Success means: every scoped issue file is fully read, triaged, and closed with concrete reasoning; every valid issue is fixed within the allowed code scope with appropriate tests; full verification passes via the repository gate; exactly one local commit is created; no out-of-scope review files are touched.

Constraints/Assumptions:

- Must follow root `AGENTS.md` / `CLAUDE.md` plus the batch execution contract from the user prompt.
- Required skills in use: `cy-fix-reviews` and `cy-final-verify`.
- Must read every listed issue file completely before editing code.
- Must not call provider-specific scripts, `gh` mutations, or other external resolution commands.
- Automatic commits are enabled only after clean verification.
- Current worktree may contain unrelated changes; do not revert or modify them.

Key decisions:

- Reuse the prior E2E session ledgers for transport parity, network collaboration, runtime sandbox, and lane wiring as context only; this batch is strictly review remediation.
- Triage all 20 issues first before changing code so overlapping findings can be fixed coherently.
- Close non-actionable style-only findings directly in their issue files when they do not hold up against the repository rules or would require brittle message matching.

State:

- Scoped remediation complete; full repository verification blocked by unrelated pre-existing test asset failure.

Done:

- Loaded the required `cy-fix-reviews` and `cy-final-verify` skill instructions.
- Read review-round metadata from `.compozy/tasks/e2e/reviews-001/_meta.md`.
- Scanned relevant prior ledgers for the transport parity, network collaboration, runtime sandbox, and e2e lane work.
- Read all 20 scoped issue files completely, including the long autogenerated CodeRabbit thread for issue 008.
- Inspected the cited code paths in every scoped source file.
- Updated every scoped issue file out of `pending` and finished the scoped set `001` through `020` as `resolved`, with concrete triage and verification notes.
- Implemented the valid fixes in the scoped Go files:
  - typed prompt finish payloads instead of map-based JSON assertions,
  - transport parity tests parallelized and hardened with explicit delivery cardinality checks and context-aware wait helpers,
  - sandbox diagnostics test assertions narrowed to observable runtime behavior,
  - network collaboration helpers now escape channel names correctly and use deterministic polling,
  - correlation assertions now require all expected attributes within a single message and include a negative regression test,
  - MCP resource tests now use focused subtests, sentinel errors, and `errors.Is` instead of string matching,
  - e2e lane plans now deep-copy package slices and include a regression test.
- Verified focused package coverage with fresh successful commands:
  - `go test ./internal/api/httpapi ./internal/api/udsapi`
  - `go test ./internal/daemon`
  - `go test ./internal/e2elane ./internal/extensiontest`
- Reran `make verify` after the final code changes. The scoped packages stayed green, but the full gate still fails in unrelated pre-existing packages `internal/testutil/acpmock` and `internal/testutil/e2e` because `internal/testutil/acpmock/driver/dist/index.js` is missing from this branch.
- Re-scanned `.codex/ledger` and refreshed the related transport parity, network collaboration, runtime sandbox, and e2e lane ledgers for cross-agent context.

Now:

- Decide final handoff and commit handling under the clean-verification requirement.

Next:

- If the unrelated `acpmock` driver asset is restored and `make verify` passes cleanly, create exactly one local commit for this batch.
- Otherwise report the verified blocker without creating a commit, because the user contract requires clean verification before commit.

Open questions (UNCONFIRMED if needed):

- The batch is implementation-complete, but the commit step is blocked until the missing `internal/testutil/acpmock/driver/dist/index.js` asset is restored or the verification gate is otherwise made clean by an in-scope direction.

Working set (files/ids/commands):

- `.codex/ledger/2026-04-17-MEMORY-e2e-review-fix.md`
- `.compozy/tasks/e2e/reviews-001/_meta.md`
- `.compozy/tasks/e2e/reviews-001/issue_001.md` through `issue_020.md`
- `internal/api/httpapi/{handlers_test.go,prompt.go,transport_parity_integration_test.go}`
- `internal/api/udsapi/transport_parity_integration_test.go`
- `internal/daemon/{daemon_environment_sandbox_integration_test.go,daemon_mock_agents_integration_test.go,daemon_network_collaboration_integration_test.go,network_e2e_assertions_test.go,tool_mcp_resources_test.go}`
- `internal/e2elane/{command_wiring_test.go,lanes.go,lanes_test.go}`
- `internal/extensiontest/bridge_adapter_harness_test.go`
- `make verify`
- `go test ./internal/api/httpapi ./internal/api/udsapi`
- `go test ./internal/daemon`
- `go test ./internal/e2elane ./internal/extensiontest`
